export interface Config {
    id: string
    adminUser: boolean
    web: string
    name: string
    street: string
    city: string
    phone: string
    nif: string
    email: string
    info: string[]
    conditions: string,
    garanty: string
}