export interface Company {
    id: string
    icon: string
    web: string
    name: string
    street: string
    city: string
    phone: string
    nif: string
    email: string
    header: string[]
    conditions: string,
    garanty: string
}